## Intsall Typst 

Go into terminal (WSL on windows ) and brew install typst or snap install snap (linux)

## to generate the preview, 

intsall Tinymist Typst VS Code Extension

1. open the file you want to see:
2. hit: cmd + shift + P (palette comand)
3. select preview

if you select generate pdf, it will