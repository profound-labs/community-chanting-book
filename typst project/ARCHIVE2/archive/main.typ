

#let a(it) = {
  box[
    #it
    #place(center+top, dy:-4pt)[#image("up2.svg", width: 5pt)]
  ]
}
#let b(it) = {
  box[
    #it
    #place(center+bottom, dy:5pt, rotate(180deg)[#image("up2.svg", width: 5pt)])
  ]
}

He has pointed out the Dh#a[a]mma: 

beautiful in the beg#b[i]nning, 

beautiful in the m#a[i]ddle, beautiful #b[i]n the end.

